# Session handoff — 2026-06-18

A continue-here note so work can resume cleanly **from any account / machine** (this captures things that otherwise live only in one assistant's local memory).

## TL;DR
Live in prod on Fly (`app.onlinejourno.com`, auth-gated, verified). Inflow pipeline healthy on the markets/regulatory wedge. Working branch **`slice/ia-realignment`** is **4 commits ahead of `main`** (clean tree). This session shipped the first Trends chart + the demo-corpus wiring; the open decision is what to build next (vendor-neutral cleanup #108 vs the Interest Trajectory chart #107).

## Branch / deploy state
- Branch **`slice/ia-realignment`**, ahead of `main` by:
  - `958e212` genericize empty-state copy (vendor-neutral, #108)
  - `6056414` Topic Momentum bar chart (Plotly, ADR 0061, #107)
  - `2bb8025` The Hindu demo-corpus seed + empty-states (#104)
  - `ebe4590` README status → prod-live + this handoff
- **Deploy is from `main`.** To ship the above: `git checkout main && git merge --ff-only slice/ia-realignment && fly deploy`.

## Durable principles (canonical — honour these; sources in repo)
- **Vendor-neutral / The Hindu is DEMO ONLY.** Any outlet configures the platform and the *entire* surface (name, content, sources, IA, every functionality) reflects *that* outlet. Outlet identity comes from `tenants.name` + `tenants.config`; **never** hardcode "The Hindu"/"thehindu.com" in source. The Hindu lives only in seeds + a configured demo tenant. (CLAUDE.md rule 5; PRODUCT.md vendor-neutral.) Known source leaks tracked in **#108**.
- **Trends/intel surfaces must replicate the `localhost:8501` Streamlit (discover-dashboard) GRAPHICALLY** — not just tables: Editorial Overview (bars+signal+prediction), **Interest Trajectory line chart**, **Topic Momentum bar chart**, Story Clusters, Channel/Entity Affinity. Tracker **#107**; charting capability now exists (ADR 0061, Plotly).
- **Locale-relative scoring** — Story Scores/Potential are market-aware + competitor-relative (a US trend ≠ relevant to an India outlet).
- **Ground-up** — surfaces educate the reporter (show the *why*/implications), not just route orders.
- **IA north-star** — Calendar = the planning spine; flat discovery nav (supersedes the old four-job ADR 0058); The Hindu is the test case.
- **Editorial attribution** — commission / "take it up" attributes to the journalist, never the admin.

## Shipped this session
- **Trends — Topic Momentum bar chart (Plotly).** `ADR 0061` adopts `react-plotly.js` (dist-min via factory, dynamic `ssr:false`, themed to tokens, lazy-loaded). `components/charts/{Plotly,MomentumBar}.tsx` + a section in `apps/web/app/[locale]/trends/page.tsx`. Type-check + prod build green.
- **The Hindu demo corpus.** `infra/seeds/demo_sources.sql` (Hindu GDELT broad + Business GDELT + RSS, `self` tenant, idempotent) + empty-states on `/potential`, `/gems`, dashboard.
- **Vendor-neutral copy fix.** Dropped `thehindu.com` from user-facing empty-state copy.
- **Earlier in session:** stage-docs alignment; auth audit (**#103 closed** — auth is enforced; the "admin without login" was a stale automation-browser cookie); 13 issues filed.

## In progress / next decision
- **#107 Interest Trajectory line chart** — needs the hour-by-hour interest series + **{outlet}** News/Discover markers (read tenant name, NOT "The Hindu"). Then enrich the Editorial Overview table; confirm Story Clusters.
- **#108 vendor-neutral cleanup** — move outlet domain + section/IA map into `tenants.config`; pages/prompts read `tenant.name`/`config`. Touches keyword fetch + competitor authority → proper slice, not a hack. Leaks: `trends/page.tsx:108` `outletDomain="thehindu.com"`; `scoring-py/advisory.py` Hindu in an LLM prompt; `agents-py/prompts.py` Hindu section/desk map.
- Build flow chosen earlier: **subagent-driven**.

## Pending ops (your machine — not runnable from here)
To light up the back-engine surfaces (#104) with the Hindu demo:
1. `git checkout main && git merge --ff-only slice/ia-realignment && fly deploy` (ships charts + empty-states).
2. `psql "$DATABASE_URL" -f infra/seeds/demo_sources.sql` against prod.
3. set `OJ_DEMO_HOST=thehindu.com` in the worker env.
4. run one pipeline cycle (`collect → enrich → frame → stories-from-signals --host thehindu.com → score-stories → site-crawl`).
- View the new Momentum chart: needs a dev-server restart locally (new Plotly dep) or the deploy above.

## Open backlog (GitHub Issues — the continue-here list)
- **#108** vendor-neutral: remove hardcoded The Hindu from source.
- **#107** Trends: replicate 8501 graphical views (Momentum done; **Interest Trajectory next**).
- **#106** IA naming overlaps (Story Scores vs Hidden Gems vs Surface Scores; Signals vs EIP Signals) — decision.
- **#105** reader-need drift (4-need on /signals vs 6-need on homepage).
- **#104** no published-story corpus in prod (the ops steps above close it).
- **#101** `packages/modules/` empty — ADR 0006 module contract.
- **#100** reporter-on-phone PWA — unproven success-test persona.
- **#99** wedge data — largely addressed in prod inflow; verify + close.
- **#98** AI-Overviews surface — likely overtaken by story-analyser; verify + close.
- **#97 / #96 / #95** Scores count mismatch; per-article scoring; Newslist seed links.
- **#103** CLOSED (auth false alarm).

## Key file pointers
- Charts: `apps/web/components/charts/`; chart ADR: `docs/adr/0061-charting-library-plotly.md`.
- Trends: `apps/web/app/[locale]/trends/page.tsx`; trend logic: `apps/web/lib/trends.ts` (+ `packages/agents-py/.../trend_score.py`).
- Demo corpus seed: `infra/seeds/demo_sources.sql`. Deploy runbook: `docs/DEPLOY.md`.
- Slice-1 (Calendar→Newslist) artefacts: `docs/superpowers/specs|plans/2026-06-14-calendar-newslist-fusion*.md` (shipped).
- Binding product def: `docs/PRODUCT.md`. Tenant schema: `infra/migrations/0001_init.sql` (`tenants.name`/`config`).
