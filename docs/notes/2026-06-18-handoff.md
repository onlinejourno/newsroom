# Session handoff — 2026-06-18

A continue-here note so work can resume cleanly **from any account / machine** (this captures things that otherwise live only in one assistant's local memory).

## TL;DR
Live in prod on Fly (`app.onlinejourno.com`, auth-gated, verified). Inflow pipeline healthy on the markets/regulatory wedge. Working branch **`slice/ia-realignment`** is **~7 commits ahead of `main`** (clean tree, pushed). This session: shipped **both Trends charts** (Topic Momentum + Interest Trajectory) + Editorial Overview score bars (#107), wired the demo corpus, and fixed the vendor-neutral source leaks (#108 main). **Remaining 8501 parity:** only a Google News Story Clusters web surface (clustering exists in Python `cluster_threads`, not yet wired to `/trends`).

## Branch / deploy state
- Branch **`slice/ia-realignment`** (pushed to origin), ahead of `main` by (newest first):
  - `2c3c10b` handoff — #108 vendor-neutral note
  - `3854553` vendor-neutral — outlet identity from tenant config (#108)
  - `80a2383` refresh handoff (cross-account)
  - `958e212` genericize empty-state copy (vendor-neutral, #108)
  - `6056414` Topic Momentum bar chart (Plotly, ADR 0061, #107)
  - `2bb8025` The Hindu demo-corpus seed + empty-states (#104)
  - `ebe4590` README status → prod-live + this handoff
- **Deploy is from `main`.** To ship: `git checkout main && git merge --ff-only slice/ia-realignment && fly deploy`.

## Durable principles (canonical — honour these; sources in repo)
- **Vendor-neutral / The Hindu is DEMO ONLY.** Any outlet configures the platform and the *entire* surface (name, content, sources, IA, every functionality) reflects *that* outlet. Outlet identity comes from `tenants.name` + `tenants.config`; **never** hardcode "The Hindu"/"thehindu.com" in source. The Hindu lives only in seeds + a configured demo tenant. (CLAUDE.md rule 5; PRODUCT.md vendor-neutral.) Main source leaks fixed (#108, `3854553`); a few minor ones remain.
- **Trends/intel surfaces must replicate the `localhost:8501` Streamlit (discover-dashboard) GRAPHICALLY** — not just tables: Editorial Overview (bars+signal+prediction), **Interest Trajectory line chart**, **Topic Momentum bar chart**, Story Clusters, Channel/Entity Affinity. Tracker **#107**; charting capability exists (ADR 0061, Plotly); Momentum chart done; Interest Trajectory in progress.
- **Locale-relative scoring** — Story Scores/Potential are market-aware + competitor-relative (a US trend ≠ relevant to an India outlet).
- **Ground-up** — surfaces educate the reporter (show the *why*/implications), not just route orders.
- **IA north-star** — Calendar = the planning spine; flat discovery nav (supersedes the old four-job ADR 0058); The Hindu is the test case.
- **Editorial attribution** — commission / "take it up" attributes to the journalist, never the admin.

## Shipped this session
- **Trends — Topic Momentum bar chart (Plotly).** `ADR 0061` adopts `react-plotly.js` (dist-min via factory, dynamic `ssr:false`, themed to tokens, lazy-loaded). `components/charts/{Plotly,MomentumBar}.tsx` + a section in `trends/page.tsx`. Type-check + prod build green.
- **The Hindu demo corpus.** `infra/seeds/demo_sources.sql` (Hindu GDELT broad + Business GDELT + RSS, `self` tenant, idempotent; demo domain set in `tenants.config`) + empty-states on `/potential`, `/gems`, dashboard.
- **Vendor-neutral (#108 main).** `outletDomain` reads `tenants.config` (new `tenantOutletDomain` helper); `advisory.py` takes `outlet_name`; `ENRICH_BEATS` de-branded + per-tenant `beats` override; user-facing copy de-Hindu'd. Verified (type-check + agents/scoring tests).
- **Earlier:** stage-docs alignment; auth audit (**#103 closed** — auth enforced; the "admin without login" was a stale automation-browser cookie); 14 issues filed.

## In progress / next
- **#107 Trends charts** — **Momentum bar + Interest Trajectory line both SHIPPED** (`6056414`, `ef8eebc`). Charts are platform-native: interest = our own signal corpus (NOT Google Trends — `trend_score.py` dropped pytrends); markers from `channel_affinity_log` (outlet label from tenant config). Editorial Overview now has inline score bars + signal + prediction (`93dc5f6`). **Remaining 8501 parity:** a Google News **Story Clusters** web table — the clustering exists in Python (`agents-py/cluster_threads.py`, `cmd_cluster`) but isn't surfaced in `/trends` (needs a clustered-output query + a section). New chart data: `topicInterestSeries` / `outletChannelMarkers` in `lib/db.ts`.
- **#108 remaining (minor):** wire `enrich.py` to pass tenant-config beats end-to-end; cosmetics (`scores/page.tsx:327` placeholder, `gdelt.py` name→domain map, CLI `--host` help).
- Build flow chosen earlier: **subagent-driven** (this session ran inline for momentum).

## Pending ops (your machine — not runnable from here)
To light up the back-engine surfaces (#104) with the Hindu demo:
1. `git checkout main && git merge --ff-only slice/ia-realignment && fly deploy` (ships charts + empty-states + #108).
2. `psql "$DATABASE_URL" -f infra/seeds/demo_sources.sql` against prod (sources + demo domain in config).
3. set `OJ_DEMO_HOST=thehindu.com` in the worker env.
4. run one pipeline cycle (`collect → enrich → frame → stories-from-signals --host thehindu.com → score-stories → site-crawl`).
- View the new Momentum chart: needs a dev-server restart locally (new Plotly dep) or the deploy above.

## Open backlog (GitHub Issues — the continue-here list)
- **#108** vendor-neutral: main source leaks fixed; minor remaining (per-tenant beats wiring + cosmetics).
- **#107** Trends: replicate 8501 graphical views (Momentum done; **Interest Trajectory in progress**).
- **#106** IA naming overlaps (Story Scores vs Hidden Gems vs Surface Scores; Signals vs EIP Signals) — decision.
- **#105** reader-need drift (4-need on /signals vs 6-need on homepage).
- **#104** no published-story corpus in prod (the ops steps above close it).
- **#101** `packages/modules/` empty — ADR 0006 module contract.
- **#100** reporter-on-phone PWA — unproven success-test persona.
- **#99** wedge data — largely addressed in prod inflow; verify + close.
- **#98** AI-Overviews surface — likely overtaken by story-analyser; verify + close.
- **#97 / #96 / #95** Scores count mismatch; per-article scoring; Newslist seed links.
- **#103** CLOSED (auth false alarm).

## Key file pointers
- Charts: `apps/web/components/charts/`; chart ADR: `docs/adr/0061-charting-library-plotly.md`.
- Trends: `apps/web/app/[locale]/trends/page.tsx`; trend logic: `apps/web/lib/trends.ts` (+ `packages/agents-py/.../trend_score.py`). Channel/entity affinity: `channel_affinity_log` (migration), `apps/web/lib/db.ts` `channelAffinity`.
- Demo corpus seed: `infra/seeds/demo_sources.sql`. Deploy runbook: `docs/DEPLOY.md`.
- Slice-1 (Calendar→Newslist) artefacts: `docs/superpowers/specs|plans/2026-06-14-calendar-newslist-fusion*.md` (shipped).
- Binding product def: `docs/PRODUCT.md`. Tenant schema: `infra/migrations/0001_init.sql` (`tenants.name`/`config`); outlet domain helper: `lib/db.ts` `tenantOutletDomain`.
