# Session handoff — 2026-06-18

A continue-here note so work can resume cleanly (incl. from another account).

## Where the project is
- **Live in prod on Fly:** `app.onlinejourno.com` — **auth-gated (verified)**. Web image deployed; the worker pipeline (cron) is running.
- **Inflow healthy:** ~221 classified signals / 7d, fresh today — RBI, **SEBI enforcement**, PIB, bill-tracking. On the **markets/regulatory wedge**.
- **IA realigned:** flat discovery nav; **Calendar = "Start here · the planning spine."** Home page carries the six User Needs + five scholarship lenses (PEJ, Deuze, User Needs, SEJ Periodic Table, Google SQEG).
- **Shipped recently:** Slice 1 (Calendar→Newslist fusion — `calendar_fuse.py` + Suggested column); ADR 0059 Phase A (production tasks + plan logic); story-analyser (SEO + E-E-A-T); trends enrichment (Top Keywords, channel affinity); local-pulse; topic→domains; design-consistency foundation + UI-kit guide.

## Auth audit result (the #103 correction)
- `apps/web/middleware.ts` requires a valid session on all routes except `OPEN = [login, register, accept, pending]`; redirects to `/login` otherwise. **Independent of `DEMO_SIGNIN`.**
- `apps/web/lib/auth.ts`: scrypt passwords, httpOnly + secure session cookie, `status='approved'` gating.
- **Verified** by a cookieless fetch of `/en` → returns the **login page**. Auth is enforced.
- The earlier "admin without login" was a **persisted session cookie in the automation browser**, not open auth. **#103 closed** as not-a-bug.
- Hardening (low priority): confirm `DEMO_SIGNIN` unset in prod (login page shows the real sign-in → likely already off); `/api/*` bypasses middleware by design (only `/health` today) — future data APIs must call `getAccount()` themselves.

## Open backlog (GitHub Issues — the continue-here list)
Roughly priority order:
- **#104** (bug) — no published-story corpus in prod → Story Scores / Hidden Gems / home dashboard blank. Wire `OJ_DEMO_HOST` or the CMS connector.
- **#105** (bug) — reader-need drift: `/signals` uses the 4-need model; the homepage uses the 6-need model. Propagate 6-need.
- **#106** (decision) — IA naming overlaps: Story Scores vs Hidden Gems vs Surface Scores; Signals vs EIP Signals.
- **#95 / #96 / #97** (bugs) — Newslist seed links; Scores per-article scoring; Scores count mismatch.
- **#98** (enh) — likely overtaken by story-analyser; verify + close.
- **#99** (enh) — wedge data; largely addressed in prod inflow; verify + close.
- **#100** (question) — reporter-on-phone PWA: still the unproven success-test persona.
- **#101** (enh) — `packages/modules/` empty; ADR 0006 module contract.
- **#103** (security) — **CLOSED**, false alarm (auth enforced).

## Continue from here
- Build flow chosen: **subagent-driven** execution of plans.
- Pre-pilot priorities: **#104** (so the back-engine surfaces populate), create the real design-partner account, then **#105 / #106**.
- Slice-1 artefacts: `docs/superpowers/specs|plans/2026-06-14-calendar-newslist-fusion*.md` (shipped).
- This session was review/triage only — no app code changed by me; 11 issues filed (#95–101, #103–106), #103 closed.
