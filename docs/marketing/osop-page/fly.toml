app = "onlinejourno-osop"
primary_region = "bom"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[[http_service.checks]]
  method = "get"
  path = "/"
  interval = "30s"
  timeout = "5s"
