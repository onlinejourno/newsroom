# onlinejourno-worker

Placeholder. The Python worker process — cron-driven collectors, scheduled
jobs, and any future agent runners that live outside the Next.js request
cycle — gets built here in Wk 2+.

Currently empty so the uv workspace at the repo root has a valid member.
